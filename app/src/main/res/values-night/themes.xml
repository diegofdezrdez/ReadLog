<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ReadLog" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Configuración para Edge-to-Edge -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>

        <!-- Opcional: En modo noche, los iconos de la barra de estado son claros por defecto, -->
        <!-- por lo que no es necesario "android:windowLightStatusBar=false" explícitamente -->
    </style>
</resources>
